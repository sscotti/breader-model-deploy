# Self-signed TLS material for the Caddy gateway (local / LAN deploy).
# Generate with: EXTRA_DNS=breader.medinformatics.eu ./scripts/gen_localhost_certs.sh
# Do not commit real certs/keys (see .gitignore).
